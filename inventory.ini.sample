# inventory.ini.sample
# ----------------------------------------------------------------
# Ansible 接続先リストのサンプル
# ----------------------------------------------------------------
#
# 使い方:
# 1. このファイルを `inventory.ini` という名前にコピーしてください。
# 2. サーバーの情報をあなたの環境に合わせて書き換えてください。
#
#    または、プロジェクト内の `generator.html` を使うと、
#    対話形式で簡単に `inventory.ini` を生成できます。
#
# ----------------------------------------------------------------

[servers]
# ここに「設定したいUbuntuサーバー」のIPアドレス、またはホスト名を記述します。
# 最初はDHCPなどで取得した一時的なIPアドレスを記述してください。
# 例: 192.168.1.50
change_this.server.ip

[servers:vars]
# ここに、サーバーへ「最初に接続するときに使うユーザー名」を記述します。
#
# 一般的なレンタルサーバーやVPSの場合:
# ansible_user=root
#
# AWS EC2 の Ubuntu インスタンスの場合:
# ansible_user=ubuntu
#
ansible_user=root